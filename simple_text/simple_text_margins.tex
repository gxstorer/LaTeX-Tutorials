\documentclass{article}% [Determines font size] { }Determines type of document (article/beamer/book/etc.)

\usepackage[text={6.5in,9in},centering]{geometry}% Determines spacing rules for document. [Options for page size & margins] 

\begin{document}% Required to produce a compiled LaTeX document. 

        Geometry package sets the text frame of the document. You can set the overall textbox, and also specify the margins individually, or generally. The "centering" makes the margins equal on all sides.

\end{document}